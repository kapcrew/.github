name: Перенос задач между итерациями (без коммитов)

on:
  workflow_dispatch:
    inputs:
      source_iteration:
        description: 'Номер предыдущей итерации (например: 133). Оставьте пустым для автоопределения'
        required: false
        type: string
      target_iteration:
        description: 'Номер текущей итерации (например: 136). Оставьте пустым для автоопределения'
        required: false
        type: string
      add_comment:
        description: 'Добавить комментарий к перенесенным задачам?'
        required: false
        type: boolean
        default: true
  
  schedule:
    - cron: '*/5 * * * *'  # Каждые 5 минут

jobs:
  move-issues:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      contents: read
    
    steps:
      - name: Перенос задач между итерациями в Projects
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.PROJECT_PAT }}
          script: |
            // Функция для получения текущей итерации из Repository Variable
            async function getCurrentIteration() {
              try {
                const { data } = await github.rest.actions.getRepoVariable({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  name: 'CURRENT_ITERATION'
                });
                return parseInt(data.value);
              } catch (error) {
                console.log('⚠️  Переменная CURRENT_ITERATION не найдена, используем значение по умолчанию');
                return 136; // TODO: установите начальную итерацию
              }
            }
            
            // Функция для обновления текущей итерации
            async function updateCurrentIteration(newValue) {
              try {
                await github.rest.actions.updateRepoVariable({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  name: 'CURRENT_ITERATION',
                  value: newValue.toString()
                });
                console.log(`💾 Переменная CURRENT_ITERATION обновлена: ${newValue}`);
                return true;
              } catch (error) {
                if (error.status === 404) {
                  // Переменная не существует, создаем её
                  try {
                    await github.rest.actions.createRepoVariable({
                      owner: context.repo.owner,
                      repo: context.repo.repo,
                      name: 'CURRENT_ITERATION',
                      value: newValue.toString()
                    });
                    console.log(`✅ Переменная CURRENT_ITERATION создана: ${newValue}`);
                    return true;
                  } catch (createError) {
                    console.error(`❌ Не удалось создать переменную: ${createError.message}`);
                    return false;
                  }
                } else {
                  console.error(`❌ Не удалось обновить переменную: ${error.message}`);
                  return false;
                }
              }
            }
            
            // Функция для нормализации названия итерации
            function normalizeIterationName(input) {
              if (!input) return null;
              if (/^\d+$/.test(input.trim())) {
                return `Iterations ${input.trim()}`;
              }
              return input.trim();
            }
            
            // Определяем режим запуска
            const isScheduled = context.eventName === 'schedule';
            const currentIterationNum = await getCurrentIteration();
            
            let sourceIterationName, targetIterationName, addComment;
            
            if (isScheduled) {
              // Автоматический запуск
              const sourceNum = currentIterationNum;
              const targetNum = currentIterationNum + 1;
              
              sourceIterationName = normalizeIterationName(sourceNum.toString());
              targetIterationName = normalizeIterationName(targetNum.toString());
              addComment = true;
              
              console.log('⏰ Автоматический запуск по расписанию');
              console.log(`📊 Текущая итерация из переменной: ${sourceNum}`);
            } else {
              // Ручной запуск
              const sourceInput = '${{ github.event.inputs.source_iteration }}';
              const targetInput = '${{ github.event.inputs.target_iteration }}';
              
              if (!sourceInput && !targetInput) {
                const sourceNum = currentIterationNum;
                const targetNum = currentIterationNum + 1;
                
                sourceIterationName = normalizeIterationName(sourceNum.toString());
                targetIterationName = normalizeIterationName(targetNum.toString());
                
                console.log('👤 Ручной запуск с автоопределением итераций');
                console.log(`📊 Текущая итерация из переменной: ${sourceNum}`);
              } else {
                sourceIterationName = normalizeIterationName(sourceInput);
                targetIterationName = normalizeIterationName(targetInput);
                
                console.log('👤 Ручной запуск с указанными итерациями');
              }
              
              addComment = '${{ github.event.inputs.add_comment }}' === 'true';
            }
            
            console.log('🚀 Начинаем перенос задач в Projects');
            console.log(`📤 Источник: "${sourceIterationName}"`);
            console.log(`📥 Цель: "${targetIterationName}"`);
            console.log('---');
            
            const projectNumber = 3; // TODO: изменить на ваш номер проекта
            const owner = context.repo.owner;
            
            // Функция для получения всех элементов проекта
            async function getAllProjectItems(projectId) {
              let allItems = [];
              let hasNextPage = true;
              let cursor = null;
              
              while (hasNextPage) {
                const query = `
                  query($projectId: ID!, $cursor: String) {
                    node(id: $projectId) {
                      ... on ProjectV2 {
                        items(first: 100, after: $cursor) {
                          pageInfo {
                            hasNextPage
                            endCursor
                          }
                          nodes {
                            id
                            content {
                              ... on Issue {
                                number
                                title
                                url
                                state
                              }
                            }
                            fieldValues(first: 20) {
                              nodes {
                                ... on ProjectV2ItemFieldIterationValue {
                                  field {
                                    ... on ProjectV2IterationField {
                                      id
                                      name
                                    }
                                  }
                                  iterationId
                                  title
                                }
                              }
                            }
                          }
                        }
                      }
                    }
                  }
                `;
                
                const result = await github.graphql(query, {
                  projectId: projectId,
                  cursor: cursor
                });
                
                const items = result.node.items.nodes;
                allItems = allItems.concat(items);
                
                hasNextPage = result.node.items.pageInfo.hasNextPage;
                cursor = result.node.items.pageInfo.endCursor;
              }
              
              return allItems;
            }
            
            // GraphQL запрос для получения информации о проекте
            const projectQuery = `
              query($owner: String!, $number: Int!) {
                organization(login: $owner) {
                  projectV2(number: $number) {
                    id
                    title
                    fields(first: 20) {
                      nodes {
                        ... on ProjectV2IterationField {
                          id
                          name
                          configuration {
                            iterations {
                              id
                              title
                            }
                            completedIterations {
                              id
                              title
                            }
                          }
                        }
                      }
                    }
                  }
                }
              }
            `;
            
            let projectData;
            try {
              projectData = await github.graphql(projectQuery, {
                owner: owner,
                number: projectNumber
              });
            } catch (error) {
              core.setFailed(`❌ Ошибка получения данных проекта: ${error.message}`);
              return;
            }
            
            const project = projectData.organization.projectV2;
            const iterationField = project.fields.nodes.find(field => field.name === 'Iterations');
            
            if (!iterationField) {
              core.setFailed('❌ Поле "Iterations" не найдено в проекте');
              return;
            }
            
            console.log(`✅ Найден проект: ${project.title}`);
            
            const activeIterations = iterationField.configuration.iterations || [];
            const completedIterations = iterationField.configuration.completedIterations || [];
            const allIterations = [...activeIterations, ...completedIterations];
            
            console.log(`📊 Найдено итераций: ${allIterations.length}`);
            
            const sourceIteration = allIterations.find(i => i.title === sourceIterationName);
            const targetIteration = allIterations.find(i => i.title === targetIterationName);
            
            if (!sourceIteration) {
              core.setFailed(`❌ Итерация "${sourceIterationName}" не найдена`);
              return;
            }
            
            if (!targetIteration) {
              core.setFailed(`❌ Итерация "${targetIterationName}" не найдена`);
              return;
            }
            
            console.log(`✅ Найдена исходная итерация: ${sourceIteration.title}`);
            console.log(`✅ Найдена целевая итерация: ${targetIteration.title}`);
            console.log('---');
            
            console.log('📥 Загружаем все элементы проекта...');
            const allItems = await getAllProjectItems(project.id);
            console.log(`📊 Всего элементов в проекте: ${allItems.length}`);
            
            const itemsToMove = allItems.filter(item => {
              if (!item.content) return false;
              if (item.content.state === 'CLOSED') return false;
              
              const iterationValue = item.fieldValues.nodes.find(fv => 
                fv.field?.name === 'Iterations' && fv.iterationId
              );
              
              if (!iterationValue) return false;
              return iterationValue.iterationId === sourceIteration.id;
            });
            
            console.log(`📊 Найдено задач для переноса: ${itemsToMove.length}`);
            
            if (itemsToMove.length === 0) {
              console.log('✨ Нет задач для переноса');
              return;
            }
            
            console.log('---');
            
            let movedCount = 0;
            const movedIssues = [];
            
            const updateMutation = `
              mutation($projectId: ID!, $itemId: ID!, $fieldId: ID!, $iterationId: String!) {
                updateProjectV2ItemFieldValue(
                  input: {
                    projectId: $projectId
                    itemId: $itemId
                    fieldId: $fieldId
                    value: {iterationId: $iterationId}
                  }
                ) {
                  projectV2Item {
                    id
                  }
                }
              }
            `;
            
            for (const item of itemsToMove) {
              try {
                const issue = item.content;
                
                await github.graphql(updateMutation, {
                  projectId: project.id,
                  itemId: item.id,
                  fieldId: iterationField.id,
                  iterationId: targetIteration.id
                });
                
                if (addComment) {
                  try {
                    await github.rest.issues.createComment({
                      owner: context.repo.owner,
                      repo: context.repo.repo,
                      issue_number: issue.number,
                      body: `🔄 Задача автоматически перенесена из итерации **"${sourceIterationName}"** в **"${targetIterationName}"**\n\n_Дата переноса: ${new Date().toLocaleString('ru-RU')}_`
                    });
                  } catch (commentError) {
                    console.log(`⚠️  Не удалось добавить комментарий к #${issue.number}`);
                  }
                }
                
                movedCount++;
                movedIssues.push({
                  number: issue.number,
                  title: issue.title,
                  url: issue.url
                });
                console.log(`✅ #${issue.number}: ${issue.title}`);
                
              } catch (error) {
                console.error(`❌ Ошибка при переносе #${item.content?.number}: ${error.message}`);
              }
            }
            
            console.log('---');
            console.log(`📈 Результаты: перенесено ${movedCount} задач`);
            
            // Обновляем переменную после успешного переноса
            if (movedCount > 0) {
              const targetIterationNumber = parseInt(targetIterationName.replace(/\D/g, ''));
              
              if (targetIterationNumber) {
                await updateCurrentIteration(targetIterationNumber);
                console.log(`✅ Следующий запуск: ${targetIterationNumber} → ${targetIterationNumber + 1}`);
              }
            }
            
            console.log('🎉 Перенос завершен!');
